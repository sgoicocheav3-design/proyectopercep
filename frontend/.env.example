# URL base de la API Flask (src/api/app.py). En desarrollo local con
# `python -m src.api.app` no hace falta definir esta variable: el frontend
# usa http://localhost:5000 por defecto.
VITE_API_URL=http://localhost:5000
