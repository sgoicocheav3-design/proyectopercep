# --- Big Data / Procesamiento distribuido ---
# El cluster real (Python 3.9, seccion 3.3.3) puede usar pyspark==3.3.4 tal
# cual especifica el documento. En Windows con Python 3.11 (unico disponible
# en esta maquina de desarrollo), 3.3.4 falla al serializar funciones
# (cloudpickle incompatible con Python 3.11); se verifico que 3.5.1 funciona.
pyspark==3.5.1
pyarrow==12.0.1

# --- Deep Learning ---
tensorflow==2.13.0
# Horovod requiere MPI + CMake/compilador C++ para instalarse; no tiene
# soporte nativo en Windows (falla el build). Se instala solo en el cluster
# Linux real. En Windows, src/training/train.py cae automaticamente a un
# modo single-process (ver shim en ese archivo) si horovod no esta instalado.
horovod==0.28.1

# --- Imagenes / Numerico ---
numpy==1.24.3
pandas==2.0.3
opencv-python-headless==4.8.0.76
Pillow==10.0.0

# --- API REST ---
flask==2.3.3
flask-cors==4.0.1
gunicorn==21.2.0

# --- Utilidades ---
scikit-learn==1.3.0
matplotlib==3.7.2
tqdm==4.66.1
kaggle==1.5.16

# --- Testing (no se instala en la imagen de deploy, ver requirements-api.txt) ---
pytest==7.4.3
